services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: solid_suite_mono_db_demo
      POSTGRES_PASSWORD: password
      POSTGRES_DB: solid_suite_mono_db_demo_development
    volumes:
      - postgres_data:/var/lib/postgresql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U solid_suite_mono_db_demo -d solid_suite_mono_db_demo_development"]
      interval: 2s
      timeout: 5s
      retries: 10

  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: ./bin/rails server
    volumes:
      - .:/rails
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgres://solid_suite_mono_db_demo:password@db:5432/solid_suite_mono_db_demo_development
      RAILS_ENV: development
      SOLID_QUEUE_IN_PUMA: "1"
      BINDING: "0.0.0.0"
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
